{
  "name": "walas-examples-async",
  "private": true,
  "description": "async example.",
  "scripts": {
    "build": "browserify -d ./src/index.js -t babelify  -o ./dist/build.js",
    "watch": "watchify -v -d ./src/index.js -t babelify -o ./dist/build.js",
    "serve": "lite-server"
  },
  "main": "dist/build.js",
  "devDependencies": {
    "babel-preset-stage-3": "^6.22.0",
    "babelify": "^7.3.0",
    "browserify": "^14.1.0",
    "connect-history-api-fallback": "^1.3.0",
    "watchify": "^3.9.0"
  }
}
